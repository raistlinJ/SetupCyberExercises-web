services:
  toolhub:
    build: .
    ports:
      - "8081:8080"
    environment:
      - PORT=8080
      - DATA_DIR=/data
    restart: unless-stopped
    volumes:
      - toolhub-data:/data

volumes:
  toolhub-data:
